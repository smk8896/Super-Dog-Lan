<%= simple_form_for(@dog) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :breed %>
    <%= f.input :dob %>
    <%= f.input :weight %>
    <%= f.association :vet %>
    <%= f.input :in_daycare %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
